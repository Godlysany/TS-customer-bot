# WhatsApp CRM Bot Configuration

# Server Configuration
PORT=8080  # Backend API port (Railway deployment)
HOST=0.0.0.0

# Supabase Configuration (Required)
# Get from: Supabase Dashboard → Project Settings → API → Project API keys
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...  # service_role key (JWT format, NOT sb_secret_...)

# WhatsApp Configuration
RESET_AUTH=false
WHATSAPP_REPLY_MODE=text  # Options: text, voice, voice-on-voice

# Optional: Voice Features
DEEPGRAM_API_KEY=
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# Note: Frontend port is auto-configured (5000 for Replit, 8080 for Railway via PORT env)
# Note: OpenAI, SendGrid, and other API keys are configured in the CRM Settings page
