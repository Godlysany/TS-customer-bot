# WhatsApp CRM Bot Configuration

# Server Configuration (Railway uses port 8080)
PORT=8080
HOST=0.0.0.0

# Supabase Configuration (Required)
# Get from: Supabase Dashboard → Project Settings → API → Project API keys
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...  # service_role key (JWT format, NOT sb_secret_...)

# WhatsApp Configuration
# Set to "true" to delete auth_info and force fresh WhatsApp login
RESET_AUTH=false

# Note: OpenAI and Deepgram API keys are configured in the CRM Settings page (not here)
