# WhatsApp Bot Configuration

# Server Port (backend API)
PORT=3000

# Make.com Webhook URL (required for message processing)
MAKE_WEBHOOK_URL=https://hook.make.com/your-webhook-url

# Deepgram API Key (required for voice transcription)
DEEPGRAM_API_KEY=your-deepgram-api-key

# ElevenLabs API Configuration (required for text-to-speech)
ELEVENLABS_API_KEY=your-elevenlabs-api-key
ELEVENLABS_VOICE_ID=your-voice-id

# Reply Mode Options: "voice", "voice-on-voice", "text"
# - voice: Always reply with voice
# - voice-on-voice: Reply with voice only when user sends voice
# - text: Always reply with text
REPLY_MODE=voice-on-voice

# Set to "true" to delete auth_info and force fresh WhatsApp login
RESET_AUTH=false
